<?xml version="1.0" encoding="UTF-8"?>
<project>
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>parent</artifactId>
        <groupId>com.bramosystems.oss.player</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <artifactId>flex-mp3-player</artifactId>
    <packaging>swf</packaging>
    <name>Flex MP3 Player</name>

    <description>
        This project is component part of the BST Player API.
        It implements the FlashMP3Player flash application.
    </description>

    <build>
        <sourceDirectory>src/main/flex</sourceDirectory>
        <defaultGoal>install</defaultGoal>
        <plugins>
            <plugin>
                <groupId>net.israfil.mojo</groupId>
                <artifactId>maven-flex2-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <flexHome>${flex.home}</flexHome>
                    <useNetwork>true</useNetwork>
                    <main>Main.mxml</main>
                    <incremental>false</incremental>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
