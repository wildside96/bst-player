<?xml version="1.0" encoding="UTF-8"?>

<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/XDOC/2.0 http://maven.apache.org/xsd/xdoc-2.0.xsd">

    <properties>
        <title>Demo Applications - Windows Media Player</title>
    </properties>
    
    <body>
        <section name="Embedding Windows Media Player">
            <script type="text/javascript" src="scripts/wmp.js"></script>
            <script type="text/javascript" src="gwt-demo/jsShowcase/jsShowcase.nocache.js">
            </script>

            <p>
                The following sections contain various example codes to embed Windows
                Media Player plugin
            </p>

            <subsection name="Playing sound automatically">
                <div align="center" id="ex_1"></div>
                <source>Widget wmp = null;
try {
    wmp = new WinMediaPlayer(GWT.getHostPageBaseURL() + "media/applause.mp3");
} catch (LoadException ex) {
    Window.alert("Load exp");
} catch (PluginVersionException ex) {
    wmp = PlayerUtil.getMissingPluginNotice(Plugin.WinMediaPlayer);
} catch (PluginNotFoundException ex) {
    wmp = PlayerUtil.getMissingPluginNotice(Plugin.WinMediaPlayer);
}
panel.add(wmp);
                </source>
            </subsection>

            <subsection name="Playing sound with autoplay set to false">
                <div align="center" id="ex_2"></div>
                <source>
Widget wmp = null;
try {
    wmp = new WinMediaPlayer(GWT.getHostPageBaseURL() + "media/applause.mp3", false);
} catch (LoadException ex) {
    Window.alert("Load exp");
} catch (PluginVersionException ex) {
    wmp = PlayerUtil.getMissingPluginNotice(Plugin.WinMediaPlayer);
} catch (PluginNotFoundException ex) {
    wmp = PlayerUtil.getMissingPluginNotice(Plugin.WinMediaPlayer);
}
panel.add(wmp);
                </source>
            </subsection>

            <subsection name="Playing sound with logger widget visible">
                <div align="center" id="ex_3"></div>
                <source>
Widget wmp = null;
try {
    WinMediaPlayer p = new WinMediaPlayer(GWT.getHostPageBaseURL() +
        "media/o-na-som.mp3", false);
    p.showLogger(true);
    wmp = p;
} catch (LoadException ex) {
    Window.alert("Load exp");
} catch (PluginVersionException ex) {
    wmp = PlayerUtil.getMissingPluginNotice(Plugin.WinMediaPlayer);
} catch (PluginNotFoundException ex) {
    wmp = PlayerUtil.getMissingPluginNotice(Plugin.WinMediaPlayer);
}
panel.add(wmp);
                </source>
            </subsection>

            <subsection name="Embedding Video - Islamic Jihad (online)">
                <div align="center" id="ex_4"></div>
                <source>
Widget wmp = null;
try {
   WinMediaPlayer p = new WinMediaPlayer(
        "http://bst-player.googlecode.com/svn/tags/showcase/media/" +
        "islamic-jihad.wmv", false, "350px", "70%");
    p.showLogger(true);
    wmp = p;
} catch (LoadException ex) {
    Window.alert("Load exp");
} catch (PluginVersionException ex) {
    wmp = PlayerUtil.getMissingPluginNotice(Plugin.WinMediaPlayer);
} catch (PluginNotFoundException ex) {
    wmp = PlayerUtil.getMissingPluginNotice(Plugin.WinMediaPlayer);
}
panel.add(wmp);
                </source>
            </subsection>
        </section>
    </body>
</document>