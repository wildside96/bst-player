<?xml version="1.0" encoding="UTF-8"?>

<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/XDOC/2.0 http://maven.apache.org/xsd/xdoc-2.0.xsd">

    <properties>
        <title>Demo Applications - Flash Samples</title>
    </properties>

    <body>
        <section name="Embedding Flash Plugin">
            <script type="text/javascript" src="scripts/flash.js"></script>
            <script type="text/javascript" src="gwt-demo/jsShowcase/jsShowcase.nocache.js">
            </script>

            <p>
                The following sections contain sample codes to embed audio and video with
                the Flash plugin
            </p>

            <subsection name="Playing MP3 media automatically">
                <div align="center" id="ex_1"></div>
                <source>
Widget mp = null;
try {
    mp = new FlashMP3Player(GWT.getHostPageBaseURL() + "media/applause.mp3");
} catch (LoadException ex) {
    Window.alert("Load exp");
} catch (PluginVersionException ex) {
    mp = PlayerUtil.getMissingPluginNotice(Plugin.FlashMP3Player,
        ex.getRequiredVersion());
} catch (PluginNotFoundException ex) {
    mp = PlayerUtil.getMissingPluginNotice(Plugin.FlashMP3Player);
}
panel.add(mp);
                </source>
            </subsection>

            <subsection name="Playing sound with autoplay set to false">
                <div align="center" id="ex_2"></div>
                <source>
Widget mp = null;
try {
    mp = new FlashMP3Player(GWT.getHostPageBaseURL() + "media/thunder.mp3", false);
} catch (LoadException ex) {
    Window.alert("Load exp");
} catch (PluginVersionException ex) {
    mp = PlayerUtil.getMissingPluginNotice(Plugin.FlashMP3Player,
        ex.getRequiredVersion());
} catch (PluginNotFoundException ex) {
    mp = PlayerUtil.getMissingPluginNotice(Plugin.FlashMP3Player);
}
panel.add(mp);
                </source>
            </subsection>

            <subsection name="Embedding sound with logger widget visible">
                <div align="center" id="ex_3"></div>
                <source>
Widget mp = null;
try {
    FlashMP3Player p = new FlashMP3Player(GWT.getHostPageBaseURL() +
        "media/o-na-som.mp3", false);
    p.showLogger(true);
    mp = p;
} catch (LoadException ex) {
    Window.alert("Load exp");
} catch (PluginVersionException ex) {
    mp = PlayerUtil.getMissingPluginNotice(Plugin.FlashMP3Player,
        ex.getRequiredVersion());
} catch (PluginNotFoundException ex) {
    mp = PlayerUtil.getMissingPluginNotice(Plugin.FlashMP3Player);
}
panel.add(mp);
                </source>
            </subsection>

            <subsection name="Embedding Flash Video - vixy.net Podcaster Demo (online)">
                <div align="center" id="ex_4"></div>
                <source>
Widget mp = null;
try {
    FlashVideoPlayer v1 = new FlashVideoPlayer("http://vixy.net/flv/vixy_net_" +
        "podcaster_demo_director.flv", false, "350px", "100%");
    v1.showLogger(true);
    mp = v1;
} catch (LoadException ex) {
    Window.alert("Load exp");
} catch (PluginVersionException ex) {
    mp = PlayerUtil.getMissingPluginNotice(Plugin.FlashVideoPlayer,
        ex.getRequiredVersion());
} catch (PluginNotFoundException ex) {
    mp = PlayerUtil.getMissingPluginNotice(Plugin.FlashVideoPlayer);
}
panel.add(mp);
                </source>
            </subsection>
        </section>
    </body>
</document>